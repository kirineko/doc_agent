## ADDED Requirements

### Requirement: 侧栏检查更新入口

系统 SHALL 在左侧栏提供「检查更新」可点击入口，位于全局配置区域（与会话无关），样式与现有侧栏控件一致。

#### Scenario: 侧栏展示检查更新

- **WHEN** 用户打开应用主窗口
- **THEN** 左侧栏可见「检查更新」入口

#### Scenario: 检查进行中反馈

- **WHEN** 用户触发检查更新且请求尚未完成
- **THEN** 入口或邻近区域展示进行中状态（如禁用或 loading），防止重复触发
