## 1. 持久化

- [x] 1.1 `activeWorkspace.ts` read/write/clear + 单测
- [x] 1.2 `resolveInitialSessionId` + 单测

## 2. 集成

- [x] 2.1 `selectProject` / `setActiveSessionId` 写入缓存
- [x] 2.2 启动 `list_projects` 后恢复一次
