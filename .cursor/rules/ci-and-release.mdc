---
description: CI/CD 与 Win/macOS 双平台发布规范，流水线必须跑测试
alwaysApply: true
---

# CI 与发布

## CI（PR 门禁）

- **触发**：`pull_request` → `main`（**push main 不触发**）
- **平台**：`ubuntu-latest`（仅跑测试与前端构建，不打包安装包）

1. **Rust**：`cargo fmt --check`、`cargo clippy -- -D warnings`、`cargo test`
2. **前端**：`npm run typecheck`、`npm test`、`npm run build`
3. **脚本 bundle**：`npm run bundle:js`

任一失败则禁止合并。

## 发布流水线（Release）

- **触发**：推送**纯数字三段版本 tag**（**不带 `v` 前缀**），匹配 `*.*.*`
- **格式**：**SemVer 兼容日历版本（CalVer）`YYYY.M.D`** — 年.月.日，**各段禁止前导零**（用 `2026.6.14`，勿用 `2026.06.14`）
- **流程**：先跑 check → 各平台并行 `tauri build` 上传 artifact → **单一 publish job** 汇总发布（避免 matrix 并行调用 `tauri-action` 互相覆盖 Release 资源）
- **目标平台**：Windows（x86_64，`.msi` / `.nsis`）+ macOS（aarch64，`.dmg`）
- **不支持 Linux** 安装包
- **产物**：GitHub Release 附件 + 阿里云 OSS `releases/<version>/` 与根 `latest.json`
- **禁止**：未打 tag 的生产构建、跳过 test 的 release、带 `v` 前缀的 tag（如 `v2026.6.14`）

## 版本号与 tag 规范

- Git tag 与 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 的 `version` **完全一致**
- **CalVer 语义**：MAJOR = 公历年，MINOR = 月（1–12），PATCH = 日（1–31）；示例 `2026.6.1`、`2026.6.14`、`2026.12.31`
- **禁止前导零**：`6` 月写 `6` 非 `06`；9 日写 `9` 非 `09`（SemVer 2.0 与 Tauri updater 要求）
- **展示**：Release 标题 / CHANGELOG 可用 `2026-06-14`；tag 与 `latest.json` 仍用 `2026.6.14`
- **历史**：已发布 SemVer（`1.0.0`、`1.0.1`）保留；自下一版起使用 CalVer。`2026.6.14` 数值上大于 `1.0.1`，updater 可正常升级
- 细则见 OpenSpec：`openspec/changes/adopt-calver-versioning/`（能力 `project-versioning`）

## 本地合并前自检

```bash
cd src-tauri && cargo fmt --check && cargo clippy -- -D warnings && cargo test
cd .. && npm run typecheck && npm test && npm run build && npm run bundle:js
```

## 发版命令示例

```bash
# 1. 将 package.json、Cargo.toml、tauri.conf.json 的 version 设为发布日 YYYY.M.D（例：2026.6.14）
git tag 2026.6.14
git push origin 2026.6.14
```

CI 配置文件：`.github/workflows/ci.yml`、`.github/workflows/release.yml`。
