---
description: 需求与变更管理遵循 OpenSpec proposal / apply / archive 流程
alwaysApply: true
---

# OpenSpec 需求管理

所有功能变更必须先走 OpenSpec，禁止跳过提案直接大改代码。

## 流程

| 阶段 | 命令/动作 | 产出 |
|------|-----------|------|
| 探索 | `/opsx-explore` | 调研结论（可选） |
| 提案 | `/opsx-propose` 或 `openspec/changes/<name>/` | `proposal.md`、`design.md`、`specs/**`、`tasks.md` |
| 实现 | `/opsx-apply` | 按 `tasks.md` 逐项完成并勾选 |
| 归档 | `/opsx-archive` | 变更归档，能力 spec 合并到 `openspec/specs/` |

## 规则

- 新能力：先在 `openspec/changes/<change>/specs/` 写 ADDED Requirements（含 Scenario）
- 实现与 `design.md` 冲突时，先更新 artifact 再改代码
- 每完成一项 task，立即在 `tasks.md` 标记 `[x]`
- MVP 范围以 `proposal.md` 的「纳入 / 排除」为准，不擅自扩 scope

## 示例

```text
# ✅ 正确：先提案再实现
openspec/changes/add-export-pdf/proposal.md → tasks.md → 写代码

# ❌ 错误：直接加功能无 change、无 spec delta
直接在 src-tauri 加 export_pdf 命令
```
