---
description: 前端类型检查、测试与组件规范（src）
globs: src/**/*
alwaysApply: false
---

# 前端质量

## 必过检查

提交前必须通过（与 CI 一致）：

- `npm run typecheck`（`tsc --noEmit`）
- `npm test`（Vitest）
- `npm run build`

CI 与 release 的 check job 另含 `npm run bundle:js`；发版规范见 `.cursor/rules/ci-and-release.mdc`。

## 结构

- `components/`：展示组件，无直接业务编排
- `hooks/`：Tauri invoke、event 订阅、状态逻辑
- `types.ts`：与 Rust 序列化结构对齐的类型定义
- 禁止在 `App.tsx` 堆超过 ~200 行逻辑，应拆 hook/组件

## 测试要求（强制）

**新增或修改 TypeScript/TSX 代码 MUST 附带或更新 Vitest 测试**；无测试的 PR 不得合并（纯样式/注释/format 除外）。

| 代码类型 | 最低要求 |
|----------|----------|
| `src/lib/**` | 纯函数与 reducer 单元测试（与源文件同目录 `*.test.ts`） |
| `src/hooks/**` | 每个 hook 至少 1 个 `renderHook` 测试（mock invoke/event） |
| `src/components/**` | 关键 UI（Markdown、工具链、输入区）组件测试 |
| IPC 契约 | types 与 event payload 形状与 Rust 一致；变更时同步更新测试 |

## 测试范围

- 工具函数、hooks：单元测试
- 关键 UI（Markdown 渲染、工具链卡片）：组件测试
- 与 Rust 的契约：types 与 event payload 形状保持一致

## 示例

```tsx
// ✅ 事件处理抽到 hook 并单测
const { liveTools, streamingContent } = useAgentEvents(sessionId);

// ❌ 在组件内 300+ 行 listen + invoke + setState 且无 hook 测试
```
